<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.montelongo.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ochoplanetas"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu" />
        <activity android:name=".descripcion" />
        <activity android:name=".cuerposCelestes" />
        <activity android:name=".intrumentos" />
        <activity android:name=".planetas" />
        <activity android:name=".movimientos" />
        <activity android:name=".actividad" />
        <activity android:name=".video" />
        <activity android:name=".resultados"></activity>
    </application>

</manifest>